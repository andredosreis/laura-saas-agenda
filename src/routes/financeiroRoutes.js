import express from 'express';
// import { } from '../controllers/financeiroController.js';

const router = express.Router();

// Rotas futuras para gestão financeira (despesas, caixa, etc)
// Por enquanto, o analytics financeiro está em analyticsRoutes.js

export default router;
